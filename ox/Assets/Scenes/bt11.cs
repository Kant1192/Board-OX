using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class bt11 : MonoBehaviour
{
    // Start is called before the first frame update
    bool AlreadyTouched = false;
    public 
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Onclick()
    {
        if (!AlreadyTouched)
        {

        }
    }
}
